# Full-Stack Website Starter

This project is a lightweight full-stack website built with plain Node.js and vanilla frontend files.

## Features

- Responsive landing page with custom styling
- Backend API for health checks and contact submissions
- Local JSON storage for submitted messages

## Run locally

```bash
node server.js
```

Then open `http://localhost:3000`.
